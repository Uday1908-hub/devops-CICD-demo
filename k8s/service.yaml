apiVersion: v1
kind: Service
metadata:
  name: cloudnative-api
  namespace: default
  labels:
    app: cloudnative-api
spec:
  type: LoadBalancer
  selector:
    app: cloudnative-api
  ports:
    - protocol: TCP
      port: 80
      targetPort: 8000
