apiVersion: v1
kind: Service
metadata:
  name: usecase-api
  namespace: default
  labels:
    app: usecase-api
spec:
  type: LoadBalancer
  selector:
    app: usecase-api
  ports:
    - protocol: TCP
      port: 80
      targetPort: 8000
